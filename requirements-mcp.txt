-r requirements.txt
# Keep the repository's currently tested LangGraph checkpoint API. LangGraph 1.x
# is not compatible with the checkpoint-sqlite <3 constraint in requirements.txt.
langgraph>=0.2,<1
langgraph-checkpoint>=2.1,<3
langchain-core>=0.3,<1
mcp==2.0.0
