[build]
builder = "DOCKERFILE"
# Kept for compatibility with the existing Railway service configuration.
# On this branch it intentionally deploys only the production-task Mock pages.
dockerfilePath = "Dockerfile"

[deploy]
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
