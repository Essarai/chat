[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
# Rely on Dockerfile CMD; do not override with a second start command.
healthcheckPath = "/health"
healthcheckTimeout = 120
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
