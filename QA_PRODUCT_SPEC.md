# 期刊问答核心任务与质量验收规格

版本：v1.0
适用角色：期刊编辑、读者/潜在作者
适用范围：现有 SQLite、Chroma、Neo4j 能够支持的期刊问答与期刊洞察能力

## 1. 使用原则

1. 一次提问可以包含多个目标；每个目标必须转换为独立、可验收的 operation。
2. 统计数字、作者、机构、题名、年份和 DOI 必须来自允许证据，生成模型不得补造。
3. 标准答案结构规定“必须回答什么”，不强制固定措辞。
4. 数据不足时允许返回 `partial` 或 `unsupported`，但必须指出缺失内容和原因。
5. 当前自然年可以展示，但默认标记为未完年；增长、新兴方向和长期稳定性排名默认只使用完整年份。

## 2. 编辑视角：5 个核心任务

### E1. 判断期刊发文结构和研究领域如何变化

**典型问题**

> 近十年期刊接受文章的领域有什么变化？哪些方向正在增长或减弱？

**用户决策**

判断期刊内容结构、优势领域和需要关注的结构性变化。

**必选 operations**

- `yearly_counts`
- `top_keywords`
- `keyword_growth`
- `topic_period_compare`
- `representative_papers_by_topic`

**标准答案结构**

1. 统计范围与数据完整性。
2. 2—4 条结论摘要。
3. 年度发文量背景，但不能用发文量趋势替代领域变化。
4. 前后期主要领域及占比对比。
5. 新增、增强、减弱、消失的候选方向。
6. 每类变化的代表论文。
7. 口径、未完年和数据限制。

**验收标准**

- 时间范围与用户要求一致；未完年明确标注。
- “领域、热点、研究方向”不得作为字面主题查询。
- 至少比较两个非空阶段，并同时给出计数或论文占比。
- 每个重点变化方向至少有 1 篇可追溯论文；没有时明确说明证据不足。
- 答案必须覆盖“变化”，不能只返回年度总发文量或静态关键词榜。

### E2. 策划专题并寻找内容抓手

**典型问题**

> 如果策划“智慧农业”专题，适合设置哪些子方向？有哪些作者和论文可以参考？

**用户决策**

判断专题与期刊历史内容是否匹配，并形成候选子方向和作者线索。

**必选 operations**

- `submission_fit`
- `topic_yearly`
- `papers_by_top_keywords`
- `authors_by_keyword`
- `institutions_by_keyword`

**标准答案结构**

1. 专题匹配结论：高、中、低，并解释判定口径。
2. 历史发文基础和近年变化。
3. 3—5 个候选子方向及其数据依据。
4. 每个子方向的代表作者、机构和论文。
5. 内容空白、数据稀疏和策划风险。
6. 下一步建议，明确标记为建议而非历史事实。

**验收标准**

- 匹配结论必须引用论文量、年份分布或相关主题数据，不得仅凭模型常识。
- 推荐的作者必须有对应主题论文关系，不能由全刊高产作者榜替代。
- 推荐论文必须包含题名、年份和 DOI；缺 DOI 时明确标记。
- 子方向必须与证据中的关键词或论文内容相关，不得凭空创造热门方向。
- “建议”与“事实”分区呈现。

### E3. 识别核心作者、团队及方向演化

**典型问题**

> 近十年核心作者和研究团队有哪些？他们的研究方向发生了什么变化？

**用户决策**

发现稳定贡献者、潜在专题组织者和团队方向变化。

**必选 operations**

- `top_authors`
- `papers_for_authors`
- `author_direction_evolution`
- `author_collaborators`

**标准答案结构**

1. 时间范围和核心作者判定口径。
2. 作者排名：姓名、稳定 author_id、发文量。
3. 按作者或团队分节展示早期、中期、近期方向。
4. 主要合作者和合作强度。
5. 各阶段代表论文。
6. 同名消歧和数据不足说明。

**验收标准**

- 排名按去重 DOI 计算，排序、名称、计数和时间范围齐全。
- “团队”必须由实际共同署名关系确定，不能将主题相似作者自动视为团队。
- 方向演化至少需要 3 篇论文和 2 个非空阶段；否则返回 `partial`。
- 代表论文必须属于对应 author_id 和时间阶段。
- 不得将不同 author_id 的同名作者合并。

### E4. 判断机构长期贡献和合作结构

**典型问题**

> 哪些机构长期保持高产？主要机构之间有哪些稳定合作？

**用户决策**

识别稳定稿源机构和机构合作网络。

**必选 operations**

- `institution_stability`
- `top_institutions`
- `institution_network`
- `representative_papers_by_institution`

**标准答案结构**

1. 时间范围和“长期稳定”的定义。
2. 稳定性排名：活跃年份率、年度 Top10 率、中位发文量、最长连续活跃年份、总发文量。
3. 主要机构之间的合作关系和共同论文数。
4. 代表论文。
5. 机构别名合并与数据限制。

**验收标准**

- 默认使用最近 10 个完整年份；显式时间条件优先。
- 标记“稳定高产”必须满足活跃年份率不低于 80%、Top10 年份率不低于 50%。
- 合作关系必须由共同署名论文证明。
- 机构别名需要规范化；无法确认的别名不得强制合并。
- 至少 5 个完整年份才允许给出长期稳定性结论。

### E5. 发现新兴方向和内容缺口

**典型问题**

> 根据近十年数据，哪些方向可能成为下一阶段热点？期刊目前有哪些内容缺口？

**用户决策**

为选题规划和约稿方向提供候选线索，而不是确定性预测。

**必选 operations**

- `keyword_growth`
- `topic_period_compare`
- `papers_by_top_keywords`
- `representative_authors_by_topic`

**标准答案结构**

1. 候选方向结论，并明确使用“候选、可能、值得关注”。
2. 增长信号：前后期计数、论文占比和占比变化。
3. 持续性信号：近期出现年份数。
4. 每个候选方向的代表作者和论文。
5. 内容缺口：数据稀疏但与已有优势领域相邻的方向。
6. 预测限制和外部数据缺口。

**验收标准**

- 新兴关键词默认满足：前期不超过 1 篇、后期至少 3 篇、后期至少 2 个年份出现。
- 排名使用论文占比增量，不仅使用绝对篇数增长。
- 当前未完年不参与增长排名。
- “未来热点”必须表述为推测，不得包装成已发生事实。
- 不能从现有期刊数据推断市场规模、政策趋势或全学科趋势，除非补充外部证据。

## 3. 读者/潜在作者视角：5 个核心任务

### R1. 快速了解一个主题及其代表文献

**典型问题**

> 近五年该刊关于“马克思”的研究有哪些？主要关注什么问题？

**用户决策**

判断该主题在期刊中的覆盖范围，并获得可继续阅读的论文清单。

**必选 operations**

- `topic_papers`
- `topic_yearly`
- `authors_by_keyword`

**标准答案结构**

1. 检索主题、时间范围和命中数量。
2. 主题概览和主要子方向。
3. 年度变化。
4. 代表作者。
5. 论文列表：题名、作者、年份、DOI、全文链接。
6. 主题匹配口径和遗漏风险。

**验收标准**

- 所有论文满足主题和时间条件，并按 DOI 去重。
- 题名、作者、年份和 DOI 与数据源一致。
- 主题概括只能基于命中论文的题名、关键词或摘要。
- 默认列出全部命中论文；超过容量时显示 `X/M` 并保存 continuation cursor。
- 不得用全刊论文或全刊作者排名替代主题结果。

### R2. 了解某位作者的论文、方向和合作关系

**典型问题**

> 徐建明发表过哪些论文？研究方向和主要合作者有哪些？

**用户决策**

形成作者研究画像并找到代表文献。

**必选 operations**

- `author_profile`
- `author_papers`
- `author_topic_summary`
- `author_collaborators`

**标准答案结构**

1. 作者身份和消歧结果。
2. 发文概况：首末发表年份、发文量。
3. 主要方向及对应论文数量。
4. 主要合作者及共同论文数量。
5. 按年份降序的论文列表。
6. 同名、机构和数据限制。

**验收标准**

- 使用稳定 author_id 查询，不能仅靠姓名字符串拼接结果。
- 发文量与论文列表去重后的 DOI 数一致。
- 合作者必须有共同 DOI；共同论文数可追溯。
- 研究方向必须有对应论文证据。
- 存在多个同名 author_id 且无法消歧时必须先澄清。

### R3. 比较两个主题或两个时期

**典型问题**

> 比较该刊前十年和后十年的热点变化，或者比较传统农业与智能农业。

**用户决策**

理解两个阶段或两个主题之间的差异，而不是分别获得两份孤立列表。

**必选 operations**

- `topic_period_compare`
- `topic_yearly`
- `representative_papers_by_topic`

**标准答案结构**

1. 比较对象和时间划分。
2. 核心差异摘要。
3. 对照表：各对象/阶段的篇数、占比、变化。
4. 新增、增强、减弱、消失方向。
5. 双方代表论文。
6. 可比性和样本量限制。

**验收标准**

- 两个比较对象必须完整保留，不能退化为全刊热门词榜。
- 两个阶段应连续、无重叠；奇数年份时近期阶段多 1 年。
- 使用论文占比控制年度总发文量变化。
- 至少两个非空阶段才允许给出变化结论。
- 每项差异都能追溯到阶段统计或代表论文。

### R4. 找到一个主题中的核心作者、机构和合作网络

**典型问题**

> 水稻研究中有哪些核心作者和机构？他们之间如何合作？

**用户决策**

发现该主题的主要研究力量和合作关系。

**必选 operations**

- `authors_by_keyword`
- `institutions_by_keyword`
- `author_network`
- `institution_network`
- `coauthored_papers`

**标准答案结构**

1. 主题和时间范围。
2. 核心作者排名及主题论文数。
3. 核心机构排名及主题论文数。
4. 主要合作关系和共同论文数。
5. 代表合作论文。
6. 网络边界和数据限制。

**验收标准**

- 作者、机构排名只统计主题命中论文。
- 网络节点必须来自主题 ResultSet，不得混入全刊无关节点。
- 每条合作边必须至少有 1 篇共同论文。
- 排名和网络使用相同的时间、主题条件。
- “核心”仅表示当前数据中的高产或高合作度，不能等同于学术影响力。

### R5. 判断投稿匹配度并找到参考论文

**典型问题**

> 我的研究方向是植物基因编辑，这本期刊适合投稿吗？有哪些相关论文可以参考？

**用户决策**

判断选题与期刊历史内容是否匹配，并找到投稿前需要阅读的论文。

**必选 operations**

- `submission_fit`
- `topic_papers`
- `topic_yearly`
- `submission_guidance`

**标准答案结构**

1. 匹配度结论及置信说明。
2. 历史相关论文数量、最近发表年份和趋势。
3. 期刊已覆盖的相邻方向。
4. 代表论文列表。
5. 投稿定位建议。
6. 不确定性和非录用承诺声明。

**验收标准**

- 匹配度必须由相关论文数量、时间分布和主题相似性支撑。
- 没有直接命中时，应区分“无直接证据”和“存在相邻主题”。
- 投稿建议不得承诺录用，不得虚构栏目、审稿周期或接受率。
- 代表论文必须满足主题条件并可追溯。
- 如果数据仅覆盖历史发表，必须说明无法代表当前编辑政策。

## 4. 多轮会话规则

### 4.1 状态边界

- 会话按 `(conversation_id, journal_id)` 隔离。
- 每轮保存原问题、TurnIntent、QueryPlan、约束、有序 ResultSet、OperationResult、回答和 continuation cursor。
- 切换期刊不得继承任何作者、论文、机构、主题或时间条件。
- `reset=true` 清空当前会话；修改历史问题时删除被修改轮次之后的状态。

### 4.2 约束优先级

固定使用以下优先级：

1. 当前问题明确给出的对象、主题和时间。
2. 当前问题中的选择器，如“第 3 位、前两位”。
3. 被引用上一轮的对象集合和约束。
4. 对应任务的产品默认值。

当前问题的显式条件与继承条件冲突时，以当前条件为准，并在答案中显示最终采用的范围。

### 4.3 指代规则

| 用户表达 | 解释规则 |
|---|---|
| “其、他们、这些作者、上述作者” | 指上一轮完整 author ResultSet |
| “这些论文、上述论文” | 指上一轮完整 paper ResultSet，按 DOI 引用 |
| “这些机构” | 指上一轮完整 institution ResultSet |
| “第 3 位、前两位、最后一位” | 按上一轮有序 ResultSet 选择，顺序不得重新计算 |
| “其中发文最多的” | 在上一轮集合内排序，不得回到全库排名 |
| “他/她/该作者” | 上一轮只有一个作者时直接引用；存在多个作者时澄清 |
| “分别、各自” | 对上一轮集合逐对象执行同一 operation |
| “列出、展开、具体有哪些” | 对上一轮对象执行详情 operation，不重新执行原排名 |
| “按年份排序” | 保留上一轮论文集合，只改变 presentation/sort |
| “继续” | 使用上一轮 cursor 续传，不重新查询、不重复已展示结果 |

没有可引用的上一轮 ResultSet 时，不得猜测指代对象。

### 4.4 时间继承规则

- 新问题默认不继承上一轮时间范围。
- `expand`、`select`、`filter`、`sort`、`summarize`、`continue` 默认继承目标轮次的时间范围。
- 追问显式给出新时间时，用新时间覆盖继承时间，但保留被引用对象集合。
- “近 N 年”定义为包含当前自然年的 N 个日历年，例如 2026 年的“近 5 年”为 2022—2026。
- “近 N 个完整年份”不包含当前自然年，例如 2026 年为 2021—2025。
- “近年来”默认按近 5 年处理，并在答案中显示实际年份。
- “长期”默认按最近 10 个完整年份处理。
- 当前自然年默认标记为未完年，不参加增长、新兴方向和长期稳定性排名；用户明确要求时可以纳入并提示偏差。

### 4.5 新问题与追问判定

满足任一条件时优先判为追问：

- 使用上一轮对象指代或序号选择器；
- 请求展开、筛选、排序、比较、总结或继续上一轮结果；
- 当前问题缺少独立对象，但可以由上一轮唯一确定。

满足以下条件时优先判为新问题：

- 明确提出新的实体类型和任务，例如从作者论文转向机构排名；
- 当前问题已经包含完整对象、时间和任务，不依赖上一轮才能理解；
- 使用“另外、再问一个、新问题”等切换表达。

不能仅凭句子长短或是否出现代词判断追问。

### 4.6 必须澄清的情况

- 多对象结果后使用“他/她/该作者”，但没有选择器。
- 同名作者对应多个 author_id，现有机构或论文信息无法消歧。
- “第 N 位”超出上一轮结果范围。
- “继续”时没有可用 cursor。
- 用户使用“某作者、某方向”等占位表达，无法确定真实对象。
- 两种合理解释会改变目标对象、时间范围或必选 operations。
- 模型理解置信度低于 0.65；置信度为 0.65—0.84 且存在会改变结果的歧义。

以下情况不必澄清，可以直接降级回答：

- 某个数据源暂时不可用，但其他证据足以完成部分 operation。
- 现有数据不支持被引次数、国家或国际合作判断；明确标记 `unsupported`。
- 结果为空；说明检索口径并给出可选的相邻主题，不把相邻主题伪装成直接命中。

## 5. 质量指标

三个主指标采用“精确率、召回率、事实正确性”相互制约，避免通过增加无关内容提高表面完整度。

### 5.1 回答相关度（Answer Relevance Precision，ARP）

**定义**

回答中与用户显式目标或有效继承目标直接相关的实质性答案单元，占全部实质性答案单元的比例。

**答案单元**

一个标题分节、表格、图表、排名、论文列表或独立结论视为一个答案单元；统计范围和必要限制说明不算无关内容。

**公式**

```text
ARP = 相关答案单元数 / 全部实质性答案单元数
```

部分相关单元按 0.5 计。例如用户问主题变化，回答了一张全刊发文量图但没有解释它与主题变化的关系，该单元计 0.5。

**目标**

- 离线 Eval 平均 `ARP >= 0.95`。
- 单题硬门禁 `ARP >= 0.90`。
- 出现错误对象、错误期刊或无关全库排名时，该题直接失败。

### 5.2 必选目标覆盖率（Required Goal Coverage，RGC）

**定义**

Query Plan 中必选 operations 被证据支持并在最终答案中呈现的程度。

**计分**

- `complete` 且答案包含对应结果：1 分。
- `partial`、答案呈现已有证据并说明缺失：0.5 分。
- `unsupported`、`error`、完全遗漏或只有空泛说明：0 分。
- operation 已执行但答案未呈现：0 分。

**公式**

```text
RGC = 必选 operations 得分之和 / 必选 operations 数量
```

**目标**

- 数据可支持的核心 10 任务：`RGC = 1.00`。
- 全量离线 Eval 平均 `RGC >= 0.90`。
- 任一必选 operation 没有 OperationResult 状态时，该题直接失败。

### 5.3 证据准确率（Evidence Accuracy，EA）

**定义**

回答中的可验证事实，与本轮允许证据完全一致的比例。

**事实原子**

- 高风险事实：数字、排名、年份、作者、机构、论文题名、DOI、对象关系。
- 普通事实：基于题名、关键词或摘要形成的主题总结。
- 推断：趋势解释、投稿建议、未来候选方向；必须标记为推断或建议。

**加权公式**

```text
EA = 正确事实权重之和 / 全部可验证事实权重之和
```

高风险事实权重为 2，普通事实权重为 1。无法从允许证据验证的事实计为错误；明确标记且没有新增事实的限制说明不进入分母。

**目标**

- 数字、DOI、论文、作者和机构准确率必须为 `100%`。
- 全部事实加权 `EA >= 0.98`。
- 虚构 DOI、论文、作者、机构或错误时间范围时，该题直接失败。

### 5.4 数据源权威顺序

- SQLite：论文身份、DOI、题名、作者、机构、年份、计数和排名的主事实源。
- Neo4j：作者和机构关系展示；关系必须能回溯到共同 DOI，冲突时以 SQLite 为准。
- Chroma：题名、摘要、正文片段的语义检索和总结，不作为统计总量的权威来源。
- 生成模型：只负责理解、谨慎综合和表达，不是结构化事实来源。

### 5.5 配套诊断指标

以下指标不代替三个主指标，用于定位原因：

- Intent 准确率：`正确 TurnIntent 数 / 总 Turn 数`。
- Plan 完整率：`包含全部预期必选 operations 的计划数 / 总计划数`。
- ResultSet 正确率：对象 ID、排序和约束均正确的 ResultSet 比例。
- 追问引用准确率：正确引用目标轮次和对象集合的追问比例。
- 澄清准确率：应澄清且澄清、无需澄清且直接执行的轮次比例。
- 降级透明率：发生 partial/unsupported/error 时明确向用户说明的比例，目标 100%。

## 6. Eval 最小记录格式

每个用例至少声明：

```yaml
id: E1-01
role: editor
question: 近十年期刊接受文章的领域有什么变化？
expected_intent:
  kind: new_question
  action: query
required_operations:
  - yearly_counts
  - top_keywords
  - keyword_growth
  - topic_period_compare
forbidden_topics:
  - 热点
  - 研究方向
constraints:
  relative_years: 10
required_answer_sections:
  - 统计范围
  - 领域变化
  - 增强与减弱方向
  - 代表论文
hard_gates:
  - no_invented_fact
  - correct_time_range
  - all_operations_have_status
  - answer_covers_all_complete_operations
```

上线验收时同时输出 Intent、Query Plan、OperationResult、ResultSet、CoverageReport、最终答案和三个主指标，不能只比较最终文本。
